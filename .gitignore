state/kernel_pins.json
state/pending_observations.jsonl
state/sandbox_runs/
state/staleness_checks.jsonl
__pycache__/
*.pyc
.env
*.env
!*.env.example

# Python build artifacts -- genuine gap found 2026-08-27 while testing
# a packaging fix directly (pip wheel build), confirmed these weren't
# excluded and would have been picked up by `git add -A`.
build/
dist/
*.egg-info/
