fastapi>=0.110
uvicorn>=0.29
websockets>=12.0
pydantic>=2.6
python-dotenv>=1.0
anthropic>=0.34
openai>=1.35
google-genai>=0.1

# Desktop only, optional: uvicorn[standard] instead of plain uvicorn
# pulls in uvloop/httptools for better performance. Skip this on
# Termux -- see README's Termux section.
